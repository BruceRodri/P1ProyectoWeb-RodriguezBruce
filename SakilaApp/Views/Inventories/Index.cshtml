@model IEnumerable<SakilaApp.Models.Inventory>
@{
    ViewData["Title"] = "Inventarios";
}
<h1>Inventarios</h1>
<p>
    <a asp-action="Create" class="btn btn-primary">
        Crear nuevo
        registro
    </a>
</p>
<table class="table table-striped table-bordered">
    <thead class="table-dark">
        <tr>
            <th>ID</th>
            <th>Película ID</th>
            <th>Tienda ID</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.InventoryId</td>
                <td>@item.FilmId</td>
                <td>@item.StoreId</td>
                <td>
                    <a asp-action="Details" asp-route-id="@item.InventoryId" class="btn btn-info btn-sm text-white">
                        <i class="fas fa-eye me-1"></i> Detalles
                    </a>
                    <a asp-action="Edit" asp-route-id="@item.InventoryId"
                       class="btn btn-warning btn-sm">Editar</a>
                    <a asp-action="Delete" asp-route-id="@item.InventoryId"
                       class="btn btn-danger btn-sm">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<nav class="mt-4">
    <ul class="pagination justify-content-center">
        <li class="page-item @(ViewBag.CurrentPage == 1 ? "disabled" : "")">
            <a class="page-link shadow-sm" href="@Url.Action("Index", new { page = ViewBag.CurrentPage - 1 })">
                <i class="fas fa-chevron-left"></i> Anterior
            </a>
        </li>

        @for (int i = 1; i <= ViewBag.TotalPages; i++)
        {
            if (i == 1 || i == ViewBag.TotalPages || (i >= ViewBag.CurrentPage - 2 && i <= ViewBag.CurrentPage + 2))
            {
                <li class="page-item @(i == ViewBag.CurrentPage ? "active" : "")">
                    <a class="page-link shadow-sm" href="@Url.Action("Index", new { page = i })">@i</a>
                </li>
            }
            else if (i == ViewBag.CurrentPage - 3 || i == ViewBag.CurrentPage + 3)
            {
                <li class="page-item disabled"><span class="page-link">...</span></li>
            }
        }

        <li class="page-item @(ViewBag.CurrentPage == ViewBag.TotalPages ? "disabled" : "")">
            <a class="page-link shadow-sm" href="@Url.Action("Index", new { page = ViewBag.CurrentPage + 1 })">
                Siguiente <i class="fas fa-chevron-right"></i>
            </a>
        </li>
    </ul>
</nav>